from setuptools import setup

setup(
    name='preprocessing',
    version='1.0',
    description='NLP preprocessing',
    author='DS in Prod',
    install_requires=['tensorflow', 'transformers']
    )